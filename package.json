{
  "name": "version-match",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "main.ts",
  "scripts": {
    "node-compile": "bun build main.ts",
    "compile": "bun build main.ts --compile --outfile version-match",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.0.10"
  }
}
